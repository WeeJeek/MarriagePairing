export class UserManager{
  constructor(){

  }

  /**verify_user_authorization(data, app){
    console.log("verifying auth in user manager");
    wx.login({
      success: res =>{

      }
    });
    wx.getSetting({
      success: data => {
        console.log("get setting");
        if(data.authSetting['scope.userInfo']){
          console.log("is authenticated");
          app.setData({is_authenticed: true});

        }else{
          //not authed
          app.setData({is_authenticed: false});
        }
      }
    });
  }**/

  //variables

}