import MBTIResultCalculationStrategy from "../classes/MBTIResultCalculationStrategy";

describe("testing rules of MBTI test", ()=>{
  let _sut;

  beforeEach(() => {
    _sut = new MBTIResultCalculationStrategy();
  })

  it("the rule should calculate result correctly", ()=>{

  })
})